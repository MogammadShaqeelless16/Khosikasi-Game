name: khosikasi_game
description: A new Flutter project with an African theme to inspire entrepreneurship.

publish_to: 'none' # Prevent accidental publishing.

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0' # Update if your Dart version is different.

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.1.2
  shared_preferences: ^2.1.1
  flutter_svg: ^2.0.7
  animated_text_kit: ^4.2.2
  page_view_indicators: ^2.0.0
  flutter_casual_games: ^0.1.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # Uncomment and add paths if assets or fonts are used:
  # assets:
  #   - assets/images/
  #   - assets/icons/
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
